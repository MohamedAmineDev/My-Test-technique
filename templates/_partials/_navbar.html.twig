<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{path('app_home')}}">Quantic Factory</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        {% if path == "app_home" %}
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{path('app_home')}}">Home</a>
        </li>
        {% else %}
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="{{path('app_home')}}">Home</a>
        </li>
        {% endif %}
        {% if path == "app_orders" %}
        <li class="nav-item">
          <a class="nav-link active" href="{{path('app_orders')}}">Orders</a>
        </li>
        {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_orders')}}">Orders</a>
        </li>
        {% endif %}
        {% if path == "app_contacts" %}
        <li class="nav-item">
          <a class="nav-link active" href="{{path('app_contacts')}}">Contacts</a>
        </li>
        {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_contacts')}}">Contacts</a>
        </li>
        {% endif %}
        {% if path == "app_articles" %}
        <li class="nav-item">
          <a class="nav-link active" href="{{path('app_articles')}}">Articles</a>
        </li>
        {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_articles')}}">Articles</a>
        </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>